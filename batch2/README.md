# batch2 —— 可复刻 AI 论文候选（S/A/B 档）

从 2025–2026 年 340 本目标期刊（275 本研究刊实扫）的 33 万篇论文中，
经两轮筛选与逐篇全文抽取得到的候选集。**每一篇都读了 PDF 原文**，不是自动摘要。

## 内容

| 文件 | 位置 | 说明 |
|---|---|---|
| `信息抽取batch2.xlsx` | 本仓库 | 汇总表，14 字段 + 复刻优先级/复刻分/基准公开/PDF 文件名 |
| 论文 PDF | **Google Drive（另附链接）** | 文件名前缀 = 档位 + 序号（如 `S001_`、`A012_`），与 xlsx 的「PDF 文件名」列一一对应 |

> PDF 共 270 篇、1.6GB，体积过大不入库，改由 Google Drive 分发。
> 已对 11 篇超大 PDF 做过内嵌图降采样重压（最大一篇 303MB → 18.6MB），文字层与页面渲染均完好，最大文件现为 28.5MB。

## 入选标准

必须**同时**满足：

1. 提出 agent 系统 / AI 系统 / 模型
2. 有明确的、AI 可复现的 benchmark pipeline
3. **测试集公开可下载**（部分公开也算，如 29 个测试集里公开 10 个）
4. **评估可自动化** —— 排除湿实验、硬件测评、人工评测为主判据的
5. **论文中报告了可对比的 SOTA 数值**

## 复刻优先级

`可自动化等级(40) + 基准公开度(25) + SOTA标注(10) + 期刊影响力 + 链接与公开基准数 + 头顶空间大(12)`，
再扣除人评/湿实验/硬件依赖。分档：S ≥88，A ≥78，B ≥66。

本批共 **270 篇**：S 18 / A 107 / B 145。C 档 101 篇按约定未抽取。

## 表格新增列

| 列 | 说明 |
|---|---|
| `JCR IF` | 期刊影响因子 |
| `期刊领域` | 期刊所属学科（来自期刊清单） |
| `任务领域` | 论文的技术任务领域，20 类（与「领域」列同值） |
| `文章类型` | `原创方法` 239 篇（提出算法/模型）· `纯基准评测` 31 篇（只提供评测集与横向排名，无自研待超越方法） |
| `SOTA可对比性` | `完整` 252 篇（有具体数值+对比对象）· `部分` 2 篇（仅排名或定性）· `无可对比数值` 16 篇（需人工核表，详见已知问题） |

**已剔除**：综述/复现报告 1 篇（`Reusability Report`，非原创研究）。

**自动化等级**：`A+` 可执行验证（单元测试/Pass@k）> `A` 有 ground truth 的客观指标 >
`B` 需 LLM-as-judge > `C` 需人评/湿实验/硬件（已全部排除）。

## 抽取原则

- Benchmark 规模、SOTA 数值、评估指标三项**均从 PDF 正文抽取**，抽不到就明确标注，不做推测
- `复刻备注` 里 ★ 表示推荐程度，⚠️ 标注限制（数据不公开、算力门槛、指标陷阱等）

## 已知问题

- **16 篇「无可对比数值」**：论文本身未给出可直接对比的跨方法 SOTA 数值（或数值只在图/图片表中，程序无法解析），
  这类无法满足「跑同样测评对比其 SOTA」的要求，**接入前需人工开 PDF 核对**。可用 `SOTA可对比性` 列筛出。

- 另有 3 篇全文只取到 EPMC 的 JATS XML（表格与图注被剥离，抽出的信息不全），**本批未纳入**：
  `10.1016/j.landig.2025.100926`、`10.1093/nsr/nwaf255`、`10.1186/s40168-025-02080-3`
- 8 篇 PDF 为 arXiv 版而非出版社官方版（IEEE 不做金色 OA，官方版需机构权限）：
  `A026 RingMoE`、`A035 RAW-Adapter`、`A057 DREAM`、`A066 BlackboxBench` 等

## 值得优先看的几类

**"纠错型"论文** —— 证明既有 SOTA 数字不可信，直接决定你的基线怎么设：
`GEMS`（删掉蛋白信息仍有竞争力，说明是配体记忆）、`DirLinkBench`（简单 MLP 就到 80–90%）、
`PathoROB`（病理基座在学医学中心而非病理）、`PoseBench`（AF3 在 75% 以上情况下失败）、
`DIDgen`（训练集 MAE 低 ≠ 生成分布上可靠）

**效率型** —— 复刻门槛低：
`RETFound-Green`（算力少 400 倍）、`Rein++`（只训 0.4% 参数、24GB 显存）、
`SPARK`（总成本约 4,000 欧元）、`TAMP`（5 张切片微调即超基线）

**超过硬基线** ：
`FLOWR.ROOT`（超过 FEP+ 金标准）、`DMS-Fold`（88% 靶标优于 AlphaFold2）、
`GDRNPP`（纯 RGB 超过 RGB-D+ICP）、`Biomni-R0-8B`（8B 超过 Claude 4 Sonnet）
