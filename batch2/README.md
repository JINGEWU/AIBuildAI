# batch2 —— 可复刻 AI 论文候选（S/A/B 档）

从 2025–2026 年 340 本目标期刊（275 本研究刊实扫）的 33 万篇论文中，
经两轮筛选与逐篇全文抽取得到的候选集。**每一篇都读了 PDF 原文**，不是自动摘要。

## 内容

| 文件 | 说明 |
|---|---|
| `信息抽取batch2.xlsx` | 汇总表，254 行；14 字段 + JCR IF/领域/类型/SOTA可对比性/复刻优先级/复刻分/基准公开/PDF 文件名 |
| `pdfs_batch2/` | 254 篇论文 PDF，与 xlsx 的「PDF 文件名」列**一一对应，不多不少** |

PDF 文件名前缀 = 档位 + 序号（如 `S001_`、`A012_`）。共 1.50GB，
已对超大 PDF 做过内嵌图降采样重压（最大一篇 303MB → 18.6MB），文字层与页面渲染完好，当前最大 32.2MB。

## 入选标准

必须**同时**满足：

1. 提出 agent 系统 / AI 系统 / 模型
2. 有明确的、AI 可复现的 benchmark pipeline
3. **测试集公开可下载**（部分公开也算，如 29 个测试集里公开 10 个）
4. **评估可自动化** —— 排除湿实验、硬件测评、人工评测为主判据的
5. **论文中报告了可对比的 SOTA 数值**

## 复刻优先级

`可自动化等级(40) + 基准公开度(25) + SOTA标注(10) + 期刊影响力 + 链接与公开基准数 + 头顶空间大(12)`，
再扣除人评/湿实验/硬件依赖。分档：S ≥88，A ≥78，B ≥66。

本批共 **254 篇**：**S 16 / A 102 / B 136**。C 档按约定未抽取。

**自动化等级**：`A+` 可执行验证（单元测试/Pass@k）> `A` 有 ground truth 的客观指标 >
`B` 需 LLM-as-judge > `C` 需人评/湿实验/硬件（已全部排除）。

## 表格新增列

| 列 | 说明 |
|---|---|
| `JCR IF` | 期刊影响因子 |
| `期刊领域` | 期刊所属学科（来自期刊清单） |
| `任务领域` | 论文的技术任务领域（与「领域」列同值） |
| `文章类型` | `原创方法` 223 篇（提出算法/模型）· `纯基准评测` 31 篇（只提供评测集与横向排名，无自研待超越方法） |
| `SOTA可对比性` | `完整` 238 篇（有具体数值+对比对象）· `部分` 2 篇（仅排名或定性）· `无可对比数值` 14 篇（论文未给出可直接对比的跨方法数值，接入前需人工核对） |
