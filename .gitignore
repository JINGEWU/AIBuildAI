.DS_Store
~$*

# ── 本地工作区，不上传 ──
data/*
fulltext/
fulltext copy/
arxiv_隔离/
.bak/
logs/
_剔除_*/
*.xlsx
*.csv
!batch2/*.xlsx
!batch3/*.xlsx

# 顶层散落的中间产物
下载脚本_*.js
官方全文_整合/
非官方_隔离/
fulltext_xml/

# 上面的 !batch2/*.xlsx
!batch3/*.xlsx 例外会连 Excel 锁文件一并放行，须在最后再排除一次
batch2/~$*
batch2/*.zip
batch3/~$*
batch3/*.zip

# data/ 整体不入库（works_merged.jsonl 等太大），但这三个是人和模型的判断，
# 程序重算不出来 —— 交付表全靠它们生成，丢了得重判 582 篇、重读 396 篇 PDF。
!data/verdicts2.json
!data/extracted.json
!data/baseline_verdicts.json
